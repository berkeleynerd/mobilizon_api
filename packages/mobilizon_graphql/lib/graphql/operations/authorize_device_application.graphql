mutation AuthorizeDeviceApplication($clientId: String!, $userCode: String!) {
  authorizeDeviceApplication(clientId: $clientId, userCode: $userCode) {
    clientId
    id
    name
    scope
    website
  }
}
