mutation UpdateActivitySetting($enabled: Boolean!, $key: String!, $method: String!) {
  updateActivitySetting(enabled: $enabled, key: $key, method: $method) {
    enabled
    key
    method
    user {
      id
      activitySettings {
        enabled
        key
        method
      }
    }
  }
}

