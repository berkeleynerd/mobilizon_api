mutation RemoveRelay($address: String!) {
  removeRelay(address: $address) {
    actor {
      avatar {
        ...removeRelay_MediaFields
      }
      banner {
        ...removeRelay_MediaFields
      }
      domain
      followersCount
      followingCount
      id
      local
      manuallyApprovesFollowers
      mediaSize
      name
      preferredUsername
      summary
      suspended
      type
      url
    }
    approved
    id
    insertedAt
    notify
    targetActor {
      avatar {
        ...removeRelay_MediaFields
      }
      banner {
        ...removeRelay_MediaFields
      }
      domain
      followersCount
      followingCount
      id
      local
      manuallyApprovesFollowers
      mediaSize
      name
      preferredUsername
      summary
      suspended
      type
      url
    }
    updatedAt
  }
}

fragment removeRelay_MediaFields on Media {
  alt
  contentType
  id
  metadata {
    blurhash
    height
    width
  }
  name
  size
  url
}

